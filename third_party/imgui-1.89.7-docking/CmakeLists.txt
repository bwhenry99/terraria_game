project(imgui)

add_library(imgui)
file(GLOB ${PROJECT_NAME}_SRCS CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp")
target_sources(imgui PRIVATE ${${PROJECT_NAME}_SRCS})
target_include_directories(imgui PUBLIC "${CMAKE_CURRENT_SOURCE_DIRECTORY}/")